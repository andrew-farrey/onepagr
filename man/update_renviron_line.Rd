% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto-check.R
\name{update_renviron_line}
\alias{update_renviron_line}
\title{Add or replace a single \code{KEY="value"} line in a \code{.Renviron}-style file}
\usage{
update_renviron_line(key, value, renviron_path)
}
\arguments{
\item{key}{Character. Environment variable name.}

\item{value}{Character. Value to assign (written as a quoted string).}

\item{renviron_path}{Character. Path to the file to update. Created if
it doesn't exist yet.}
}
\value{
Invisibly, \code{renviron_path}.
}
\description{
Every other line in the file is left untouched: reads the file if it
exists (an empty vector if it doesn't yet), replaces the first existing
line matching \verb{^key\\s*=} if there is one (dropping any further
duplicates, which should never legitimately exist but would be
ambiguous for R to parse if they did), or appends a new line if there's
no existing match.
}
\keyword{internal}
