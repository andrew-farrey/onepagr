% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto-check.R
\name{check_quarto}
\alias{check_quarto}
\title{Check whether Quarto is installed and its bundled Typst is new enough}
\usage{
check_quarto(min_typst_version = "0.15.1")
}
\arguments{
\item{min_typst_version}{Character. Minimum required Typst version
(the version Quarto's own bundled \code{typst} binary reports, not
Quarto's own version). Default \code{"0.15.1"}, matching this package's
own tested baseline.}
}
\value{
Invisibly, a list with \code{available} (logical), \code{quarto_version}
(\code{package_version} or \code{NA}), \code{typst_version} (\code{package_version} or
\code{NA}), and \code{ok} (logical, \code{TRUE} if available and the bundled Typst
meets \code{min_typst_version}).
}
\description{
Wraps the \code{quarto} package's \code{quarto_available()} to check Quarto is
present, then checks the \strong{Typst version Quarto actually bundles} --
not Quarto's own version number, which is NOT a reliable proxy for
this: confirmed directly (via web research plus this package's own
pinned dev environment) that Quarto sat on Typst 0.11.x across
multiple Quarto 1.4-1.7.x releases before eventually bundling a newer
one, so a Quarto-version floor can pass while the bundled Typst is
still too old for \verb{--pdf-standard ua-1} (added in Typst 0.14.0) or
\verb{--features a11y-extras} (this package is built and tested against
Typst 0.15.1 specifically, bundled by Quarto 1.10.18 -- confirmed
directly by running \verb{quarto typst --version} in the dev environment
every template in this package was verified against).
}
