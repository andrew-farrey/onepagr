% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_onepager}
\alias{render_onepager}
\title{Render a onepagr one-pager to PDF}
\usage{
render_onepager(
  data,
  template,
  theme = "default",
  theme_path = NULL,
  output,
  keep_typst = TRUE,
  extra_assets = character(0),
  font_dir = NULL
)
}
\arguments{
\item{data}{Named list of whisker substitution values. For alert-style
templates (\code{overdose_spike_alert}, \code{syndromic_alert}), the
\code{severity_level} token must be the literal lowercase string
\code{"warning"} or \code{"critical"}, and any \verb{show_*} toggle token (e.g.
\code{show_resources}, \code{show_cluster}) must be the literal lowercase
string \code{"true"} or \code{"false"}. These are substituted directly into
Typst string comparisons, so an R logical (which whisker coerces to
\code{"TRUE"}/\code{"FALSE"}, uppercase) or any other value fails the compile
loudly with a Typst \code{panic()} rather than silently rendering with
the wrong severity styling or a mis-toggled section.}

\item{template}{Character. A built-in template name (see \code{\link[=list_templates]{list_templates()}}).}

\item{theme}{Character. A built-in theme name, or a path to a custom
theme .typ file (see \code{\link[=resolve_theme]{resolve_theme()}}). Default \code{"default"}.}

\item{theme_path}{Character or \code{NULL}. Explicit theme file path
override; when supplied, \code{theme} is ignored. Default \code{NULL}.}

\item{output}{Character. Path to write the compiled PDF to.}

\item{keep_typst}{Logical. Whether to leave the resolved \code{.typ} tree
next to \code{output} (\code{TRUE}, default) or use a disposable tempdir
(\code{FALSE}).}

\item{extra_assets}{Character vector of file paths to stage into the
compile work directory alongside the theme/components/package assets
-- for per-run generated images (e.g. charts/maps produced fresh by
the calling script) that a template's own \verb{#image()} calls need to
reference. Typst's compiler sandboxes file access to the directory
being compiled from and rejects absolute filesystem paths outright
(confirmed directly: \verb{#image("C:/abs/path/map.png")} fails to
compile with "path contains invalid component" even after fixing
Windows backslashes to forward slashes -- this isn't a path-syntax
issue, Typst does not permit escaping its compile root at all). Each
file is copied in by its basename (overwriting on conflict); pass
just that basename as the corresponding whisker token's value (e.g.
\code{extra_assets = "path/to/map0.png"} pairs with a template token value
of \code{"map0.png"}, not the original full path). Default \code{character(0)}
(no extra assets, e.g. for templates whose images are all static
package assets).}

\item{font_dir}{Character or \code{NULL}. Passed through to
\code{\link[=compile_typst]{compile_typst()}}'s \code{font_dir} argument -- a directory of font files
to make available for this compile, in addition to system fonts, for
a theme's \code{text-font}/\code{heading-font} tokens that name a font not
installed system-wide (e.g. a Posit Workbench deployment without
permission to install fonts at the OS level). Default \code{NULL}.}
}
\value{
Character, the \code{output} path, invisibly.
}
\description{
The quick path: given data, a built-in template name, and a theme,
produces a finished PDF with a single call. Validates \code{data} against
the target template's required tokens before compiling (see
\code{\link[=compile_typst]{compile_typst()}}), so a missing value raises a clear R error instead
of silently rendering blank.
}
\details{
By default (\code{keep_typst = TRUE}) the resolved, whisker-substituted
\code{.typ} file is written next to \code{output}, along with the theme,
components, and assets it was compiled with -- self-contained and
independently recompilable, not hidden in a disposable tempdir. Set
\code{keep_typst = FALSE} to compile in a disposable tempdir instead and
return only the PDF.
}
