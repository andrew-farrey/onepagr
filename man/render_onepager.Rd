% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_onepager}
\alias{render_onepager}
\title{Render a onepagr one-pager to PDF}
\usage{
render_onepager(
  data,
  template,
  theme = "default",
  theme_path = NULL,
  output,
  keep_typst = TRUE
)
}
\arguments{
\item{data}{Named list of whisker substitution values.}

\item{template}{Character. A built-in template name (see \code{\link[=list_templates]{list_templates()}}).}

\item{theme}{Character. A built-in theme name, or a path to a custom
theme .typ file (see \code{\link[=resolve_theme]{resolve_theme()}}). Default \code{"default"}.}

\item{theme_path}{Character or \code{NULL}. Explicit theme file path
override; when supplied, \code{theme} is ignored. Default \code{NULL}.}

\item{output}{Character. Path to write the compiled PDF to.}

\item{keep_typst}{Logical. Whether to leave the resolved \code{.typ} tree
next to \code{output} (\code{TRUE}, default) or use a disposable tempdir
(\code{FALSE}).}
}
\value{
Character, the \code{output} path, invisibly.
}
\description{
The quick path: given data, a built-in template name, and a theme,
produces a finished PDF with a single call. Validates \code{data} against
the target template's required tokens before compiling (see
\code{\link[=compile_typst]{compile_typst()}}), so a missing value raises a clear R error instead
of silently rendering blank.
}
\details{
By default (\code{keep_typst = TRUE}) the resolved, whisker-substituted
\code{.typ} file is written next to \code{output}, along with the theme,
components, and assets it was compiled with -- self-contained and
independently recompilable, not hidden in a disposable tempdir. Set
\code{keep_typst = FALSE} to compile in a disposable tempdir instead and
return only the PDF.
}
