% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile.R
\name{template_tokens}
\alias{template_tokens}
\title{List the tokens a template takes}
\usage{
template_tokens(template)
}
\arguments{
\item{template}{Character. A built-in template name (see
\code{\link[=list_templates]{list_templates()}}), or the path to a \code{.typ} file such as one written by
\code{\link[=export_template]{export_template()}}.}
}
\value{
A data frame with one row per token and columns \code{token},
\code{required} (logical) and \code{default} (\code{NA} for a required token). Required
tokens come first, then optional ones, each in the order the template
uses them.
}
\description{
Shows every \code{{{{token}}}} a template reads: the ones your \code{data} list must
supply, and the optional ones with the value each falls back to. Use it to
find the name of a heading, label, caption or paragraph you want to
reword, then pass that name in \code{data} (see \code{\link[=render_onepager]{render_onepager()}}).
}
\details{
Optional tokens are named by what they set: \verb{heading_*} (section
headings), \verb{label_*} (box and group labels), \code{banner_label} and
\code{banner_issued} (alert banners), \verb{stat_*} (captions on numbers), \verb{bar_*}
(bar-chart row labels), \verb{text_*} (paragraphs, bullets and notes),
\verb{alt_*} (chart and map alt text), \verb{strip_label_*} and \verb{footer_label_*}
(the metadata strip and footer labels), and \verb{chips_*} (a short list of
items separated by \code{|}). Filter on those prefixes with \code{\link[=grepl]{grepl()}}.

A default is Typst markup and can itself contain \code{{{{token}}}}
references, which are filled from your data. A token that only a default
refers to still counts as required.
}
\examples{
tokens <- template_tokens("cohort_summary")
head(tokens)

# Every section heading you can reword, with its current text:
tokens[grepl("^heading_", tokens$token), c("token", "default")]
}
