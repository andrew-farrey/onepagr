% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile.R
\name{template_data}
\alias{template_data}
\title{Build a starter data list for a template}
\usage{
template_data(template, tokens = c("both", "required", "optional"))
}
\arguments{
\item{template}{Character. A built-in template name (see
\code{\link[=list_templates]{list_templates()}}). Unlike \code{\link[=template_tokens]{template_tokens()}}, this does not take
a path to a hand-edited \code{\link[=export_template]{export_template()}} copy, since there is no
known example value for a token that copy might add.}

\item{tokens}{Character. Which tokens to include: \code{"both"} (default,
everything \code{render_onepager()} would read), \code{"required"} (only the
values you must actually supply), or \code{"optional"} (only the
reworded-text and type/spacing tokens, each left at its default).}
}
\value{
Named list of character values, with a \code{"required"} attribute
naming the entries that came from a required token.
}
\description{
Returns a named list ready to pass to \code{\link[=render_onepager]{render_onepager()}} as \code{data}:
every token \code{\link[=template_tokens]{template_tokens()}} lists for \code{template}, each already set
to a real value, not a blank you have to guess the shape of -- an
optional token's own shipped default, or (for a required token, which
has no default) the matching value from that template's
\code{\link[=example_data]{example_data()}}. Starting from this instead of an empty list means
every value you haven't changed yet is already correctly formatted (a
percentage as \code{"84\%"}, a color as a real theme value, and so on),
which is the actual point: reading \code{\link[=template_tokens]{template_tokens()}}'s token names
alone tells you what to set, not what a working value for it looks
like.
}
\details{
Which entries came from a required token, as opposed to an optional
one, is recorded in the result's \code{"required"} attribute
(\code{attr(x, "required")}) rather than in the list itself, so the result
stays a plain data list: pass it straight to \code{render_onepager()},
overwrite fields directly, or merge your own values in with
\code{\link[utils:modifyList]{utils::modifyList()}}.
}
\examples{
data <- template_data("cohort_summary")
attr(data, "required")

# Change just the numbers you have; every heading and label keeps its
# default wording.
data$n_decedents <- "6,000"
\dontrun{
render_onepager(data, "cohort_summary", output = "report.pdf")
}
}
