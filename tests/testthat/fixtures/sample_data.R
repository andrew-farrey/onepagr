# Canonical content now lives in inst/examples/cohort_summary.R (see
# R/example-data.R) so it ships with the package and backs the public
# example_data()/template_data() helpers too. This file just gives it
# the name every test in this suite already expects.
sample_data <- example_data("cohort_summary")
